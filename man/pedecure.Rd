% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scaPCoA.R
\name{pedecure}
\alias{pedecure}
\title{Penalized Decomposition for Confounding Removal (PeDecURe)}
\usage{
pedecure(
  X,
  X.penalize = NULL,
  A,
  Y,
  lambda,
  nPC,
  centerX = FALSE,
  centerA = FALSE,
  centerY = FALSE,
  scaleX = FALSE,
  scaleA = FALSE,
  scaleY = FALSE
)
}
\arguments{
\item{X}{A numeric matrix of features (rows = samples, columns = features).}

\item{X.penalize}{A numeric matrix used for the penalization term. Must have
the same number of columns as \code{X}. Can be \code{NULL} if
\code{lambda = 0}.}

\item{A}{A numeric matrix of covariates to adjust for.}

\item{Y}{A numeric vector or matrix of outcome values.}

\item{lambda}{Numeric. Penalization tuning parameter. When \code{lambda = 0},
no penalization is applied (equivalent to PLS).}

\item{nPC}{Integer. Number of principal components to compute.}

\item{centerX}{Logical. Whether to center X. Default is \code{FALSE}.}

\item{centerA}{Logical. Whether to center A. Default is \code{FALSE}.}

\item{centerY}{Logical. Whether to center Y. Default is \code{FALSE}.}

\item{scaleX}{Logical. Whether to scale X. Default is \code{FALSE}.}

\item{scaleA}{Logical. Whether to scale A. Default is \code{FALSE}.}

\item{scaleY}{Logical. Whether to scale Y. Default is \code{FALSE}.}
}
\value{
A list with:
\describe{
\item{values}{A numeric vector of eigenvalues.}
\item{vectors}{A matrix of eigenvectors.}
}
Returns \code{NULL} if the penalized matrix is not symmetric.
}
\description{
Performs penalized eigen decomposition that maximizes association with the
outcome Y while penalizing association with covariates A.
}
